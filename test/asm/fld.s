fld %st(0)
fld %st(1)
fld %st(2)
fld %st(3)
fld %st(4)
fld %st(5)
fld %st(6)
fld %st(7)

fld (%edx)
fld (%rdx)

fld (%r12d)
fld (%r14)

// TODO: DD /0 FLD m64fp Valid Valid Push m64fp onto the FPU register stack.
// TODO: DB /5 FLD m80fp Valid Valid Push m80fp onto the FPU register stack.
