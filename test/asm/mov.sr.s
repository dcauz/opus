
mov %al, %es:(%ebx)
mov %ax, %es:(%rbx)
mov %eax, %es:(%rbx)
mov %rax, %es:(%rbx)

mov %es:(%ebx), %al
mov %es:(%rbx), %ax
mov %es:(%rbx), %eax
mov %es:(%rbx), %rax

mov %al, %es:100
mov %ax, %es:100
mov %eax, %es:100
mov %rax, %es:100

mov %es:100, %al
mov %es:400, %ax
mov %es:300, %eax
mov %es:200, %rax


mov %al, %fs:(%ebx)
mov %ax, %fs:(%rbx)
mov %eax, %fs:(%rbx)
mov %rax, %fs:(%rbx)

mov %fs:(%ebx), %al
mov %fs:(%rbx), %ax
mov %fs:(%rbx), %eax
mov %fs:(%rbx), %rax

mov %al, %fs:100
mov %ax, %fs:100
mov %eax, %fs:100
mov %rax, %fs:100

mov %fs:100, %al
mov %fs:400, %ax
mov %fs:300, %eax
mov %fs:200, %rax


mov %al, %gs:(%ebx)
mov %ax, %gs:(%rbx)
mov %eax, %gs:(%rbx)
mov %rax, %gs:(%rbx)

mov %gs:(%ebx), %al
mov %gs:(%rbx), %ax
mov %gs:(%rbx), %eax
mov %gs:(%rbx), %rax

mov %al, %gs:100
mov %ax, %gs:100
mov %eax, %gs:100
mov %rax, %gs:100

mov %gs:100, %al
mov %gs:400, %ax
mov %gs:300, %eax
mov %gs:200, %rax


mov %al, %ss:(%ebx)
mov %ax, %ss:(%rbx)
mov %eax, %ss:(%rbx)
mov %rax, %ss:(%rbx)

mov %ss:(%ebx), %al
mov %ss:(%rbx), %ax
mov %ss:(%rbx), %eax
mov %ss:(%rbx), %rax

mov %al, %ss:100
mov %ax, %ss:100
mov %eax, %ss:100
mov %rax, %ss:100

mov %ss:100, %al
mov %ss:400, %ax
mov %ss:300, %eax
mov %ss:200, %rax


mov %al, %cs:(%ebx)
mov %ax, %cs:(%rbx)
mov %eax, %cs:(%rbx)
mov %rax, %cs:(%rbx)

mov %cs:(%ebx), %al
mov %cs:(%rbx), %ax
mov %cs:(%rbx), %eax
mov %cs:(%rbx), %rax

mov %al, %cs:100
mov %ax, %cs:100
mov %eax, %cs:100
mov %rax, %cs:100

mov %cs:100, %al
mov %cs:400, %ax
mov %cs:300, %eax
mov %cs:200, %rax


mov %al, %ds:(%ebx)
mov %ax, %ds:(%rbx)
mov %eax, %ds:(%rbx)
mov %rax, %ds:(%rbx)

mov %ds:(%ebx), %al
mov %ds:(%rbx), %ax
mov %ds:(%rbx), %eax
mov %ds:(%rbx), %rax

mov %al, %ds:100
mov %ax, %ds:100
mov %eax, %ds:100
mov %rax, %ds:100

mov %ds:100, %al
mov %ds:400, %ax
mov %ds:300, %eax
mov %ds:200, %rax
